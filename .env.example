# Infrastructure (required for local development without Docker)
DATABASE_URL=postgresql+asyncpg://familiar:familiar@localhost:5432/familiar
REDIS_URL=redis://localhost:6379/0

# Optional: Music library path for Docker volume mounting
# Actual paths are configured via the admin UI (Settings > Library Management)
# MUSIC_LIBRARY_PATH=/path/to/your/music

# All other settings (API keys, LLM config, etc.) are configured via the admin UI:
# - API keys: /admin page
# - LLM provider: Settings > AI Assistant
# - Music library paths: Settings > Library Management
