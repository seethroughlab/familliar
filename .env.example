# Familiar Configuration
# Copy this file to .env and customize for your deployment

# Infrastructure (required)
DATABASE_URL=postgresql+asyncpg://familiar:familiar@localhost:5432/familiar
REDIS_URL=redis://localhost:6379/0

# Frontend URL (required for OAuth callbacks with Spotify/Last.fm)
# Change to your server's hostname/IP for remote access
FRONTEND_URL=http://localhost:4400

# API Keys (optional - can also be configured in Admin UI at /admin)
# ANTHROPIC_API_KEY=sk-ant-...
# SPOTIFY_CLIENT_ID=
# SPOTIFY_CLIENT_SECRET=
# LASTFM_API_KEY=
# LASTFM_API_SECRET=

# Note: Music library paths are configured via the Admin UI (Settings > Library Management)
# For Docker deployments, mount your music folder in docker-compose.yml
